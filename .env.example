# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=duosync
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/duosync?sslmode=disable

# NextAuth
NEXTAUTH_SECRET=your-secret-here-change-in-production
NEXTAUTH_URL=http://localhost:3000

# Email Configuration
# Option 1: Use EMAIL_SERVER URL format (recommended)
# Format: smtp://username:password@host:port
# For Aruba: smtp://dev@vitoesposito.it:password@smtps.aruba.it:587
EMAIL_SERVER=
EMAIL_FROM=noreply@localhost

# Option 2: Use individual SMTP variables (will be combined into EMAIL_SERVER automatically)
# These are used if EMAIL_SERVER is not set
SMTP_HOST=smtps.aruba.it
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# WebAuthn
WEBAUTHN_RP_ID=localhost

# Node Environment
NODE_ENV=development

# Development Notes:
# - In development, Mailpit is used automatically if EMAIL_SERVER is not set
# - Check http://localhost:8025 to view emails sent via Mailpit
# - For production, configure EMAIL_SERVER or SMTP_* variables