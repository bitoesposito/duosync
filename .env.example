# Database
# Per Docker: postgresql://postgres:postgres@db:5432/duosync?sslmode=disable
# Per sviluppo locale: postgresql://postgres:postgres@localhost:5432/duosync?sslmode=disable
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/duosync?sslmode=disable

# NextAuth
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Email (Mailpit)
# Per Docker: smtp://mailpit:1025
# Per sviluppo locale: smtp://localhost:1025
EMAIL_SERVER=smtp://localhost:1025
EMAIL_FROM=noreply@localhost

# Database Connection Details
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=duosync

# Node Environment
NODE_ENV=development